# KZCQL 收尾计划：遗漏修复 + Git推送 + 文章讨论准备

> 生成日期：2026-05-24
> 前置计划：KZCQL_综合改造计划_素材库与文章修改与D2D3修复.md
> 状态：待用户审核确认

---

## 摘要

前置计划的阶段一（素材库）、阶段三（D2/D3修复）、阶段四（规则更新）已全部完成，但存在3处遗漏未实施，且所有7个文件修改均未暂存/提交/推送。本计划覆盖：
1. 修复3处遗漏（D2 Step 9素材库路径、W1输入包[MUST]素材索引、W1输入包[MAY]能力画像）
2. Git暂存、提交、推送所有变更
3. 向用户展示5个候选替换故事，讨论文章修改方案

---

## 一、当前状态分析

### 1.1 已完成项（验证通过）

| 阶段 | 内容 | 状态 |
|------|------|------|
| 阶段一 | 素材库目录结构、F1-F4复制、资料包复制、素材索引.md、README.md、.gitignore | ✅ |
| 阶段三 | SOLO适配器WRITING_AGENT_SEQUENCE、W1输入包[CONDITIONAL]、编排指南、注册表 | ✅ |
| 阶段四 | 作者事实档案§5能力画像、前置撰写规则§18跨领域透镜、§16.5素材库路径 | ✅ |

### 1.2 遗漏项（验证发现）

| # | 计划要求 | 实际状态 | 影响 |
|---|----------|----------|------|
| 1 | D2调研Agent规范 Step 9 新增素材库搜索路径 | ❌ 未实施 | D2不知道去素材库查找随笔素材 |
| 2 | W1输入包模板[MUST]新增创作素材索引字段 | ❌ 未实施 | W1不知道素材库的存在 |
| 3 | W1输入包模板[MAY]新增作者能力画像字段 | ❌ 未实施 | W1不知道查阅作者事实档案§5 |

### 1.3 Git状态

- 7个文件已修改但**未暂存、未提交**
- 本地领先远程7个commit（上次会话遗留）
- 用户核心Git策略：**本地commit不安全，commit后必须立即push**

---

## 二、具体变更

### 步骤1：修复D2调研Agent Step 9素材库路径

**文件**：`/workspace/KZCQL/02_子Agent规范/调研专家组/D2_调研Agent.md`

**位置**：Step 9（约第239-244行）

**当前内容**：
```
使用 Grep 在已有知识库中搜索相关内容
路径：/workspace/KZCQL/
目的：检查是否有已积累的相关素材、历史文章、规则文档可供参考
```

**修改为**：
```
使用 Grep 在已有知识库中搜索相关内容
路径：
  1. /workspace/KZCQL/01_共享知识库/创作素材/素材索引.md（优先查阅素材索引，定位相关随笔素材）
  2. /workspace/KZCQL/（全库搜索规则文档、历史文章等）
目的：检查是否有已积累的相关素材、历史文章、规则文档可供参考
```

---

### 步骤2：W1输入包[MUST]新增创作素材索引

**文件**：`/workspace/KZCQL/00_架构文档/输入包模板/W1_初稿撰写_输入包模板.md`

**位置**：§2.1 [MUST] 表格末尾（第28行之后）

**新增行**：
```
| 创作素材索引 | `/workspace/KZCQL/01_共享知识库/创作素材/素材索引.md` | Markdown | 文件存在，涉及作者个人经历或需要真实素材时必须查阅 |
```

---

### 步骤3：W1输入包[MAY]新增作者能力画像

**文件**：`/workspace/KZCQL/00_架构文档/输入包模板/W1_初稿撰写_输入包模板.md`

**位置**：§2.4 [MAY] 表格末尾（第51行之后）

**新增行**：
```
| 作者能力画像 | `/workspace/KZCQL/01_共享知识库/作者事实档案.md` §5 | Markdown | 需要融入跨领域透镜或展示专业能力时查阅 |
```

---

### 步骤4：Git暂存、提交、推送

**操作序列**：
```bash
cd /workspace/KZCQL
git add -A
git commit -m "feat: 素材库建设 + D2/D3修复 + 规则更新 + 遗漏修复

- 新增创作素材库（素材索引.md + README.md + .gitignore排除规则）
- SOLO适配器新增WRITING_AGENT_SEQUENCE（D2->D3->W1长文流程）
- W1输入包模板新增[CONDITIONAL]分类（D2/D3长文必须）
- 作者事实档案新增§5能力画像（核心能力矩阵+跨领域透镜）
- 前置撰写规则新增§18跨领域透镜融入指南
- D2调研Agent Step 9新增素材库搜索路径
- W1输入包[MUST]新增创作素材索引、[MAY]新增作者能力画像
- 编排指南和注册表同步更新D2/D3条件触发规则"
git push origin main
```

**验证**：推送后确认远程同步成功。

---

### 步骤5：展示候选替换故事供用户讨论

从随笔素材中挖掘的5个候选替换故事（已在上次会话完成素材挖掘）：

| # | 来源 | 核心内容 | 与主题关联 |
|---|------|----------|------------|
| 1 | F2 随笔 | 600个bug→50个bug的排查经历 | 经验沉淀的方法论化 |
| 2 | F3 随笔 | 从实践到经验的闭环思考 | "做了但说不出来"的痛点 |
| 3 | F4 随笔 | "身处其中而不自知"的情感锚点 | 经验的隐性价值 |
| 4 | F1 幸福五日志 | 个人学习AI编程的踩坑记录 | 独自摸索的经历 |
| 5 | F2/F3 随笔 | KZCQL系统搭建过程中的挫折 | 从混乱到系统化的过程 |

**建议组合**：素材二（实践到经验闭环）作为框架 + 素材一（600bug→50bug细节）作为具体例证 + 素材四（"身处其中而不自知"）作为情感锚点。

**注意**：此步骤仅展示候选方案，文章实际修改（阶段二）需等用户确认方向后再执行。

---

## 三、假设与决策

| 决策点 | 选择 | 理由 |
|--------|------|------|
| 3处遗漏是单独commit还是合并 | 合并到一次commit | 这些修改属于同一批改造计划 |
| 文章修改是否在本计划执行 | 否，仅展示候选方案 | 用户明确要求"先处理完文件，再讨论文章" |

---

## 四、验证步骤

1. **D2 Step 9验证**：确认素材索引.md路径出现在Step 9中
2. **W1 [MUST]验证**：确认创作素材索引行出现在§2.1表格中
3. **W1 [MAY]验证**：确认作者能力画像行出现在§2.4表格中
4. **Git验证**：`git status` 显示工作区干净，`git log --oneline -1` 显示最新commit，远程同步确认

---

## 五、执行顺序

```
步骤1（D2 Step 9）──┐
步骤2（W1 [MUST]）──┼──→ 步骤4（Git add+commit+push）──→ 步骤5（展示候选故事）
步骤3（W1 [MAY]）───┘
```

步骤1-3可并行执行，步骤4依赖前三步完成，步骤5在Git推送后向用户展示。
